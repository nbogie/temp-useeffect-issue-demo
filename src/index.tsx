import React from "react";
import ReactDOM from "react-dom";
import WebsiteRoutes from "./Webpages/Routes";

ReactDOM.render(
  <React.StrictMode>
    <WebsiteRoutes />
  </React.StrictMode>,
  document.getElementById("root")
);
